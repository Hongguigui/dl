## Run cifar.py to generate train and evaluate model

cifar.py:
    contains all the classes and code to train the models for both cifar-10 and cifar-100

cifar_tests.py:
    unit tests for the modules

requirements.txt:
    contains all the site packages installed
